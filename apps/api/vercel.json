{
  "$schema": "https://vercel.com/schema.json",
  "version": 2,
  "buildCommand": "bun install && bun run build",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/index.js"
    }
  ]
}
